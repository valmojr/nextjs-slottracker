import Frame from '@/app/Util/Components/Divs/Frame';
import { motion } from 'framer-motion';

export default function LoadingAssignedEvents() {
  return (
    <Frame type="main" color="alternativeSecondary">
      <p>Loading Assigned Events</p>
    </Frame>
  );
}
